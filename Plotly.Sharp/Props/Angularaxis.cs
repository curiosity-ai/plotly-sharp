namespace Plotly
{
    /*////////////////////////////////
    /// THIS FILE IS AUTO-GENERATED //
    ///      by propsDocument       //
    ////////////////////////////////*/
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Types;
    using Bridge;
    using static Retyped.dom;

    public static partial class Angularaxis
    {
        /// Polar chart subplots are not supported yet. This key has currently no effect.
        public static Box<IAngularaxisProperty> domain(int val) => Interop.mkAngularaxisAttr("domain", new[]{val});
        /// Polar chart subplots are not supported yet. This key has currently no effect.
        public static Box<IAngularaxisProperty> domain(IEnumerable<int> values) => Interop.mkAngularaxisAttr("domain", values.ToArray());
        /// Polar chart subplots are not supported yet. This key has currently no effect.
        public static Box<IAngularaxisProperty> domain(float val) => Interop.mkAngularaxisAttr("domain", new[]{val});
        /// Polar chart subplots are not supported yet. This key has currently no effect.
        public static Box<IAngularaxisProperty> domain(IEnumerable<float> values) => Interop.mkAngularaxisAttr("domain", values.ToArray());
        /// A single toggle to hide the axis while preserving interaction like dragging. Default is true when a cheater plot is present on the axis, otherwise false
        public static Box<IAngularaxisProperty> visible(bool val) => Interop.mkAngularaxisAttr("visible", val);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(bool val) => Interop.mkAngularaxisAttr("categoryarray", new[]{val});
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params bool[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(System.DateTime val) => Interop.mkAngularaxisAttr("categoryarray", new[]{val});
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params System.DateTime[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(float val) => Interop.mkAngularaxisAttr("categoryarray", new[]{val});
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params float[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(int val) => Interop.mkAngularaxisAttr("categoryarray", new[]{val});
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params int[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(string val) => Interop.mkAngularaxisAttr("categoryarray", new[]{val});
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params string[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<bool[]> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(List<bool[]> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params bool[][] values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<IEnumerable<string>> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(List<string[]> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params string[][] values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<IEnumerable<int>> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(List<int[]> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params int[][] values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<IEnumerable<float>> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(List<float[]> values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(params float[][] values) => Interop.mkAngularaxisAttr("categoryarray", Bindings.flatten2DArrayIf1D(values));
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(Literals.PlotData[] values) => Interop.mkAngularaxisAttr("categoryarray", values);
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<bool?> values) => Interop.mkAngularaxisAttr("categoryarray", values.ToArray());
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<System.DateTime?> values) => Interop.mkAngularaxisAttr("categoryarray", values.ToArray());
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<int?> values) => Interop.mkAngularaxisAttr("categoryarray", values.ToArray());
        /// Sets the order in which categories on this axis appear. Only has an effect if `categoryorder` is set to *array*. Used with `categoryorder`.
        public static Box<IAngularaxisProperty> categoryarray(IEnumerable<float?> values) => Interop.mkAngularaxisAttr("categoryarray", values.ToArray());
        /// Set the angular period. Has an effect only when `angularaxis.type` is *category*.
        public static Box<IAngularaxisProperty> period(int val) => Interop.mkAngularaxisAttr("period", val);
        /// Set the angular period. Has an effect only when `angularaxis.type` is *category*.
        public static Box<IAngularaxisProperty> period(float val) => Interop.mkAngularaxisAttr("period", val);
        /// Sets that start position (in degrees) of the angular axis By default, polar subplots with `direction` set to *counterclockwise* get a `rotation` of *0* which corresponds to due East (like what mathematicians prefer). In turn, polar with `direction` set to *clockwise* get a rotation of *90* which corresponds to due North (like on a compass),
        public static Box<IAngularaxisProperty> rotation(int val) => Interop.mkAngularaxisAttr("rotation", val);
        /// Sets that start position (in degrees) of the angular axis By default, polar subplots with `direction` set to *counterclockwise* get a `rotation` of *0* which corresponds to due East (like what mathematicians prefer). In turn, polar with `direction` set to *clockwise* get a rotation of *90* which corresponds to due North (like on a compass),
        public static Box<IAngularaxisProperty> rotation(float val) => Interop.mkAngularaxisAttr("rotation", val);
        /// Sets the hover text formatting rule using d3 formatting mini-languages which are very similar to those in Python. For numbers, see: https://github.com/d3/d3-3.x-api-reference/blob/master/Formatting.md#d3_format And for dates see: https://github.com/d3/d3-3.x-api-reference/blob/master/Time-Formatting.md#format We add one item to d3's date formatter: *%{n}f* for fractional seconds with n digits. For example, *2016-10-13 09:15:23.456* with tickformat *%H~%M~%S.%2f* would display *09~15~23.46*
        public static Box<IAngularaxisProperty> hoverformat(string val) => Interop.mkAngularaxisAttr("hoverformat", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(bool val) => Interop.mkAngularaxisAttr("uirevision", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(params bool[] values) => Interop.mkAngularaxisAttr("uirevision", values);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(System.DateTime val) => Interop.mkAngularaxisAttr("uirevision", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(params System.DateTime[] values) => Interop.mkAngularaxisAttr("uirevision", values);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(int val) => Interop.mkAngularaxisAttr("uirevision", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(params int[] values) => Interop.mkAngularaxisAttr("uirevision", values);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(float val) => Interop.mkAngularaxisAttr("uirevision", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(params float[] values) => Interop.mkAngularaxisAttr("uirevision", values);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(string val) => Interop.mkAngularaxisAttr("uirevision", val);
        /// Controls persistence of user-driven changes in axis `rotation`. Defaults to `polar<N>.uirevision`.
        public static Box<IAngularaxisProperty> uirevision(params string[] values) => Interop.mkAngularaxisAttr("uirevision", values);
        /// Sets default for all colors associated with this axis all at once: line, font, tick, and grid colors. Grid color is lightened by blending this with the plot background Individual pieces can override this.
        public static Box<IAngularaxisProperty> color(string val) => Interop.mkAngularaxisAttr("color", val);
        /// Determines whether or not a line bounding this axis is drawn.
        public static Box<IAngularaxisProperty> showline(bool val) => Interop.mkAngularaxisAttr("showline", val);
        /// Sets the axis line color.
        public static Box<IAngularaxisProperty> linecolor(string val) => Interop.mkAngularaxisAttr("linecolor", val);
        /// Sets the width (in px) of the axis line.
        public static Box<IAngularaxisProperty> linewidth(int val) => Interop.mkAngularaxisAttr("linewidth", val);
        /// Sets the width (in px) of the axis line.
        public static Box<IAngularaxisProperty> linewidth(float val) => Interop.mkAngularaxisAttr("linewidth", val);
        /// Determines whether or not grid lines are drawn. If *true*, the grid lines are drawn at every tick mark.
        public static Box<IAngularaxisProperty> showgrid(bool val) => Interop.mkAngularaxisAttr("showgrid", val);
        /// Sets the color of the grid lines.
        public static Box<IAngularaxisProperty> gridcolor(string val) => Interop.mkAngularaxisAttr("gridcolor", val);
        /// Sets the width (in px) of the grid lines.
        public static Box<IAngularaxisProperty> gridwidth(int val) => Interop.mkAngularaxisAttr("gridwidth", val);
        /// Sets the width (in px) of the grid lines.
        public static Box<IAngularaxisProperty> gridwidth(float val) => Interop.mkAngularaxisAttr("gridwidth", val);
        /// Specifies the maximum number of ticks for the particular axis. The actual number of ticks will be chosen automatically to be less than or equal to `nticks`. Has an effect only if `tickmode` is set to *auto*.
        public static Box<IAngularaxisProperty> nticks(int val) => Interop.mkAngularaxisAttr("nticks", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(bool val) => Interop.mkAngularaxisAttr("tick0", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(params bool[] values) => Interop.mkAngularaxisAttr("tick0", values);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(System.DateTime val) => Interop.mkAngularaxisAttr("tick0", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(params System.DateTime[] values) => Interop.mkAngularaxisAttr("tick0", values);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(int val) => Interop.mkAngularaxisAttr("tick0", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(params int[] values) => Interop.mkAngularaxisAttr("tick0", values);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(float val) => Interop.mkAngularaxisAttr("tick0", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(params float[] values) => Interop.mkAngularaxisAttr("tick0", values);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(string val) => Interop.mkAngularaxisAttr("tick0", val);
        /// Sets the placement of the first tick on this axis. Use with `dtick`. If the axis `type` is *log*, then you must take the log of your starting tick (e.g. to set the starting tick to 100, set the `tick0` to 2) except when `dtick`=*L<f>* (see `dtick` for more info). If the axis `type` is *date*, it should be a date string, like date data. If the axis `type` is *category*, it should be a number, using the scale where each category is assigned a serial number from zero in the order it appears.
        public static Box<IAngularaxisProperty> tick0(params string[] values) => Interop.mkAngularaxisAttr("tick0", values);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(bool val) => Interop.mkAngularaxisAttr("dtick", val);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(params bool[] values) => Interop.mkAngularaxisAttr("dtick", values);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(System.DateTime val) => Interop.mkAngularaxisAttr("dtick", val);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(params System.DateTime[] values) => Interop.mkAngularaxisAttr("dtick", values);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(int val) => Interop.mkAngularaxisAttr("dtick", val);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(params int[] values) => Interop.mkAngularaxisAttr("dtick", values);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(float val) => Interop.mkAngularaxisAttr("dtick", val);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(params float[] values) => Interop.mkAngularaxisAttr("dtick", values);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(string val) => Interop.mkAngularaxisAttr("dtick", val);
        /// Sets the step in-between ticks on this axis. Use with `tick0`. Must be a positive number, or special strings available to *log* and *date* axes. If the axis `type` is *log*, then ticks are set every 10^(n*dtick) where n is the tick number. For example, to set a tick mark at 1, 10, 100, 1000, ... set dtick to 1. To set tick marks at 1, 100, 10000, ... set dtick to 2. To set tick marks at 1, 5, 25, 125, 625, 3125, ... set dtick to log_10(5), or 0.69897000433. *log* has several special values; *L<f>*, where `f` is a positive number, gives ticks linearly spaced in value (but not position). For example `tick0` = 0.1, `dtick` = *L0.5* will put ticks at 0.1, 0.6, 1.1, 1.6 etc. To show powers of 10 plus small digits between, use *D1* (all digits) or *D2* (only 2 and 5). `tick0` is ignored for *D1* and *D2*. If the axis `type` is *date*, then you must convert the time to milliseconds. For example, to set the interval between ticks to one day, set `dtick` to 86400000.0. *date* also has special values *M<n>* gives ticks spaced by a number of months. `n` must be a positive integer. To set ticks on the 15th of every third month, set `tick0` to *2000-01-15* and `dtick` to *M3*. To set ticks every 4 years, set `dtick` to *M48*
        public static Box<IAngularaxisProperty> dtick(params string[] values) => Interop.mkAngularaxisAttr("dtick", values);
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(bool val) => Interop.mkAngularaxisAttr("tickvals", new[]{val});
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<bool> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(System.DateTime val) => Interop.mkAngularaxisAttr("tickvals", new[]{val});
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<System.DateTime> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(float val) => Interop.mkAngularaxisAttr("tickvals", new[]{val});
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<float> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(int val) => Interop.mkAngularaxisAttr("tickvals", new[]{val});
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<int> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(string val) => Interop.mkAngularaxisAttr("tickvals", new[]{val});
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<string> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<bool[]> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(List<bool[]> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(params bool[][] values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<IEnumerable<string>> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(List<string[]> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(params string[][] values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<IEnumerable<int>> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(List<int[]> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(params int[][] values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<IEnumerable<float>> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(List<float[]> values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(params float[][] values) => Interop.mkAngularaxisAttr("tickvals", Bindings.flatten2DArrayIf1D(values));
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(Literals.PlotData[] values) => Interop.mkAngularaxisAttr("tickvals", values);
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<bool?> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<System.DateTime?> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<int?> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the values at which ticks on this axis appear. Only has an effect if `tickmode` is set to *array*. Used with `ticktext`.
        public static Box<IAngularaxisProperty> tickvals(IEnumerable<float?> values) => Interop.mkAngularaxisAttr("tickvals", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(bool val) => Interop.mkAngularaxisAttr("ticktext", new[]{val});
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<bool> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(System.DateTime val) => Interop.mkAngularaxisAttr("ticktext", new[]{val});
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<System.DateTime> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(float val) => Interop.mkAngularaxisAttr("ticktext", new[]{val});
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<float> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(int val) => Interop.mkAngularaxisAttr("ticktext", new[]{val});
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<int> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(string val) => Interop.mkAngularaxisAttr("ticktext", new[]{val});
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<string> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<bool[]> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(List<bool[]> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(params bool[][] values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<IEnumerable<string>> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(List<string[]> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(params string[][] values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<IEnumerable<int>> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(List<int[]> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(params int[][] values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<IEnumerable<float>> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(List<float[]> values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(params float[][] values) => Interop.mkAngularaxisAttr("ticktext", Bindings.flatten2DArrayIf1D(values));
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(Literals.PlotData[] values) => Interop.mkAngularaxisAttr("ticktext", values);
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<bool?> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<System.DateTime?> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<int?> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the text displayed at the ticks position via `tickvals`. Only has an effect if `tickmode` is set to *array*. Used with `tickvals`.
        public static Box<IAngularaxisProperty> ticktext(IEnumerable<float?> values) => Interop.mkAngularaxisAttr("ticktext", values.ToArray());
        /// Sets the tick length (in px).
        public static Box<IAngularaxisProperty> ticklen(int val) => Interop.mkAngularaxisAttr("ticklen", val);
        /// Sets the tick length (in px).
        public static Box<IAngularaxisProperty> ticklen(float val) => Interop.mkAngularaxisAttr("ticklen", val);
        /// Sets the tick width (in px).
        public static Box<IAngularaxisProperty> tickwidth(int val) => Interop.mkAngularaxisAttr("tickwidth", val);
        /// Sets the tick width (in px).
        public static Box<IAngularaxisProperty> tickwidth(float val) => Interop.mkAngularaxisAttr("tickwidth", val);
        /// Sets the tick color.
        public static Box<IAngularaxisProperty> tickcolor(string val) => Interop.mkAngularaxisAttr("tickcolor", val);
        /// Determines whether or not the tick labels are drawn.
        public static Box<IAngularaxisProperty> showticklabels(bool val) => Interop.mkAngularaxisAttr("showticklabels", val);
        /// Sets a tick label prefix.
        public static Box<IAngularaxisProperty> tickprefix(string val) => Interop.mkAngularaxisAttr("tickprefix", val);
        /// Sets a tick label suffix.
        public static Box<IAngularaxisProperty> ticksuffix(string val) => Interop.mkAngularaxisAttr("ticksuffix", val);
        /// If \"true\", even 4-digit integers are separated
        public static Box<IAngularaxisProperty> separatethousands(bool val) => Interop.mkAngularaxisAttr("separatethousands", val);
        /// Sets the tick font.
        public static Box<IAngularaxisProperty> tickfont(params Box<ITickfontProperty>[] properties) => Interop.mkAngularaxisAttr("tickfont", Bindings.flattenProperties(properties));
        /// Sets the angle of the tick labels with respect to the horizontal. For example, a `tickangle` of -90 draws the tick labels vertically.
        public static Box<IAngularaxisProperty> tickangle(int val) => Interop.mkAngularaxisAttr("tickangle", val);
        /// Sets the angle of the tick labels with respect to the horizontal. For example, a `tickangle` of -90 draws the tick labels vertically.
        public static Box<IAngularaxisProperty> tickangle(float val) => Interop.mkAngularaxisAttr("tickangle", val);
        /// Sets the tick label formatting rule using d3 formatting mini-languages which are very similar to those in Python. For numbers, see: https://github.com/d3/d3-3.x-api-reference/blob/master/Formatting.md#d3_format And for dates see: https://github.com/d3/d3-3.x-api-reference/blob/master/Time-Formatting.md#format We add one item to d3's date formatter: *%{n}f* for fractional seconds with n digits. For example, *2016-10-13 09:15:23.456* with tickformat *%H~%M~%S.%2f* would display *09~15~23.46*
        public static Box<IAngularaxisProperty> tickformat(string val) => Interop.mkAngularaxisAttr("tickformat", val);
        public static Box<IAngularaxisProperty> tickformatstops(params Box<ITickformatstopsProperty>[] properties) => Interop.mkAngularaxisAttr("tickformatstops", Bindings.flattenProperties(properties));
        /// Sets the source reference on plot.ly for  categoryarray .
        public static Box<IAngularaxisProperty> categoryarraysrc(string val) => Interop.mkAngularaxisAttr("categoryarraysrc", val);
        /// Sets the source reference on plot.ly for  tickvals .
        public static Box<IAngularaxisProperty> tickvalssrc(string val) => Interop.mkAngularaxisAttr("tickvalssrc", val);
        /// Sets the source reference on plot.ly for  ticktext .
        public static Box<IAngularaxisProperty> ticktextsrc(string val) => Interop.mkAngularaxisAttr("ticktextsrc", val);
    }

    public static partial class Angularaxis
    {
        /// Sets the angular axis type. If *linear*, set `thetaunit` to determine the unit in which axis value are shown. If *category, use `period` to set the number of integer coordinates around polar axis.
        public static partial class _type
        {
            public static Box<IAngularaxisProperty> dash() => Interop.mkAngularaxisAttr("type", "-");
            public static Box<IAngularaxisProperty> category() => Interop.mkAngularaxisAttr("type", "category");
            public static Box<IAngularaxisProperty> linear() => Interop.mkAngularaxisAttr("type", "linear");
        }

        /// Specifies the ordering logic for the case of categorical variables. By default, plotly uses *trace*, which specifies the order that is present in the data supplied. Set `categoryorder` to *category ascending* or *category descending* if order should be determined by the alphanumerical order of the category names. Set `categoryorder` to *array* to derive the ordering from the attribute `categoryarray`. If a category is not found in the `categoryarray` array, the sorting behavior for that attribute will be identical to the *trace* mode. The unspecified categories will follow the categories in `categoryarray`. Set `categoryorder` to *total ascending* or *total descending* if order should be determined by the numerical order of the values. Similarly, the order can be determined by the min, max, sum, mean or median of all the values.
        public static partial class Categoryorder
        {
            public static Box<IAngularaxisProperty> array() => Interop.mkAngularaxisAttr("categoryorder", "array");
            public static Box<IAngularaxisProperty> categoryAscending() => Interop.mkAngularaxisAttr("categoryorder", "category ascending");
            public static Box<IAngularaxisProperty> categoryDescending() => Interop.mkAngularaxisAttr("categoryorder", "category descending");
            public static Box<IAngularaxisProperty> maxAscending() => Interop.mkAngularaxisAttr("categoryorder", "max ascending");
            public static Box<IAngularaxisProperty> maxDescending() => Interop.mkAngularaxisAttr("categoryorder", "max descending");
            public static Box<IAngularaxisProperty> meanAscending() => Interop.mkAngularaxisAttr("categoryorder", "mean ascending");
            public static Box<IAngularaxisProperty> meanDescending() => Interop.mkAngularaxisAttr("categoryorder", "mean descending");
            public static Box<IAngularaxisProperty> medianAscending() => Interop.mkAngularaxisAttr("categoryorder", "median ascending");
            public static Box<IAngularaxisProperty> medianDescending() => Interop.mkAngularaxisAttr("categoryorder", "median descending");
            public static Box<IAngularaxisProperty> minAscending() => Interop.mkAngularaxisAttr("categoryorder", "min ascending");
            public static Box<IAngularaxisProperty> minDescending() => Interop.mkAngularaxisAttr("categoryorder", "min descending");
            public static Box<IAngularaxisProperty> sumAscending() => Interop.mkAngularaxisAttr("categoryorder", "sum ascending");
            public static Box<IAngularaxisProperty> sumDescending() => Interop.mkAngularaxisAttr("categoryorder", "sum descending");
            public static Box<IAngularaxisProperty> totalAscending() => Interop.mkAngularaxisAttr("categoryorder", "total ascending");
            public static Box<IAngularaxisProperty> totalDescending() => Interop.mkAngularaxisAttr("categoryorder", "total descending");
            public static Box<IAngularaxisProperty> trace() => Interop.mkAngularaxisAttr("categoryorder", "trace");
        }

        /// Sets the format unit of the formatted *theta* values. Has an effect only when `angularaxis.type` is *linear*.
        public static partial class Thetaunit
        {
            public static Box<IAngularaxisProperty> degrees() => Interop.mkAngularaxisAttr("thetaunit", "degrees");
            public static Box<IAngularaxisProperty> radians() => Interop.mkAngularaxisAttr("thetaunit", "radians");
        }

        /// Sets the direction corresponding to positive angles.
        public static partial class Direction
        {
            public static Box<IAngularaxisProperty> clockwise() => Interop.mkAngularaxisAttr("direction", "clockwise");
            public static Box<IAngularaxisProperty> counterclockwise() => Interop.mkAngularaxisAttr("direction", "counterclockwise");
        }

        /// Sets the tick mode for this axis. If *auto*, the number of ticks is set via `nticks`. If *linear*, the placement of the ticks is determined by a starting position `tick0` and a tick step `dtick` (*linear* is the default value if `tick0` and `dtick` are provided). If *array*, the placement of the ticks is set via `tickvals` and the tick text is `ticktext`. (*array* is the default value if `tickvals` is provided).
        public static partial class Tickmode
        {
            public static Box<IAngularaxisProperty> array() => Interop.mkAngularaxisAttr("tickmode", "array");
            public static Box<IAngularaxisProperty> auto() => Interop.mkAngularaxisAttr("tickmode", "auto");
            public static Box<IAngularaxisProperty> linear() => Interop.mkAngularaxisAttr("tickmode", "linear");
        }

        /// Determines whether ticks are drawn or not. If **, this axis' ticks are not drawn. If *outside* (*inside*), this axis' are drawn outside (inside) the axis lines.
        public static partial class Ticks
        {
            public static Box<IAngularaxisProperty> none() => Interop.mkAngularaxisAttr("ticks", "");
            public static Box<IAngularaxisProperty> inside() => Interop.mkAngularaxisAttr("ticks", "inside");
            public static Box<IAngularaxisProperty> outside() => Interop.mkAngularaxisAttr("ticks", "outside");
        }

        /// If *all*, all tick labels are displayed with a prefix. If *first*, only the first tick is displayed with a prefix. If *last*, only the last tick is displayed with a suffix. If *none*, tick prefixes are hidden.
        public static partial class Showtickprefix
        {
            public static Box<IAngularaxisProperty> all() => Interop.mkAngularaxisAttr("showtickprefix", "all");
            public static Box<IAngularaxisProperty> first() => Interop.mkAngularaxisAttr("showtickprefix", "first");
            public static Box<IAngularaxisProperty> last() => Interop.mkAngularaxisAttr("showtickprefix", "last");
            public static Box<IAngularaxisProperty> none() => Interop.mkAngularaxisAttr("showtickprefix", "none");
        }

        /// Same as `showtickprefix` but for tick suffixes.
        public static partial class Showticksuffix
        {
            public static Box<IAngularaxisProperty> all() => Interop.mkAngularaxisAttr("showticksuffix", "all");
            public static Box<IAngularaxisProperty> first() => Interop.mkAngularaxisAttr("showticksuffix", "first");
            public static Box<IAngularaxisProperty> last() => Interop.mkAngularaxisAttr("showticksuffix", "last");
            public static Box<IAngularaxisProperty> none() => Interop.mkAngularaxisAttr("showticksuffix", "none");
        }

        /// If *all*, all exponents are shown besides their significands. If *first*, only the exponent of the first tick is shown. If *last*, only the exponent of the last tick is shown. If *none*, no exponents appear.
        public static partial class Showexponent
        {
            public static Box<IAngularaxisProperty> all() => Interop.mkAngularaxisAttr("showexponent", "all");
            public static Box<IAngularaxisProperty> first() => Interop.mkAngularaxisAttr("showexponent", "first");
            public static Box<IAngularaxisProperty> last() => Interop.mkAngularaxisAttr("showexponent", "last");
            public static Box<IAngularaxisProperty> none() => Interop.mkAngularaxisAttr("showexponent", "none");
        }

        /// Determines a formatting rule for the tick exponents. For example, consider the number 1,000,000,000. If *none*, it appears as 1,000,000,000. If *e*, 1e+9. If *E*, 1E+9. If *power*, 1x10^9 (with 9 in a super script). If *SI*, 1G. If *B*, 1B.
        public static partial class Exponentformat
        {
            public static Box<IAngularaxisProperty> B() => Interop.mkAngularaxisAttr("exponentformat", "B");
            public static Box<IAngularaxisProperty> E() => Interop.mkAngularaxisAttr("exponentformat", "E");
            public static Box<IAngularaxisProperty> SI() => Interop.mkAngularaxisAttr("exponentformat", "SI");
            public static Box<IAngularaxisProperty> e() => Interop.mkAngularaxisAttr("exponentformat", "e");
            public static Box<IAngularaxisProperty> none() => Interop.mkAngularaxisAttr("exponentformat", "none");
            public static Box<IAngularaxisProperty> power() => Interop.mkAngularaxisAttr("exponentformat", "power");
        }

        /// Sets the layer on which this axis is displayed. If *above traces*, this axis is displayed above all the subplot's traces If *below traces*, this axis is displayed below all the subplot's traces, but above the grid lines. Useful when used together with scatter-like traces with `cliponaxis` set to *false* to show markers and/or text nodes above this axis.
        public static partial class Layer
        {
            public static Box<IAngularaxisProperty> aboveTraces() => Interop.mkAngularaxisAttr("layer", "above traces");
            public static Box<IAngularaxisProperty> belowTraces() => Interop.mkAngularaxisAttr("layer", "below traces");
        }
    }
}