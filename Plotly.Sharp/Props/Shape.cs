namespace Plotly
{
    /*////////////////////////////////
    /// THIS FILE IS AUTO-GENERATED //
    ///      by propsDocument       //
    ////////////////////////////////*/
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Types;
    using Bridge;
    using static Retyped.dom;

    public static partial class Shape
    {
        /// Determines whether or not this shape is visible.
        public static Box<IShapeProperty> visible(bool val) => Interop.mkShapeAttr("visible", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(bool val) => Interop.mkShapeAttr("xanchor", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(params bool[] values) => Interop.mkShapeAttr("xanchor", values);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(System.DateTime val) => Interop.mkShapeAttr("xanchor", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(params System.DateTime[] values) => Interop.mkShapeAttr("xanchor", values);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(int val) => Interop.mkShapeAttr("xanchor", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(params int[] values) => Interop.mkShapeAttr("xanchor", values);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(float val) => Interop.mkShapeAttr("xanchor", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(params float[] values) => Interop.mkShapeAttr("xanchor", values);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(string val) => Interop.mkShapeAttr("xanchor", val);
        /// Only relevant in conjunction with `xsizemode` set to *pixel*. Specifies the anchor point on the x axis to which `x0`, `x1` and x coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `xsizemode` not set to *pixel*.
        public static Box<IShapeProperty> xanchor(params string[] values) => Interop.mkShapeAttr("xanchor", values);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(bool val) => Interop.mkShapeAttr("x0", val);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(params bool[] values) => Interop.mkShapeAttr("x0", values);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(System.DateTime val) => Interop.mkShapeAttr("x0", val);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(params System.DateTime[] values) => Interop.mkShapeAttr("x0", values);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(int val) => Interop.mkShapeAttr("x0", val);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(params int[] values) => Interop.mkShapeAttr("x0", values);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(float val) => Interop.mkShapeAttr("x0", val);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(params float[] values) => Interop.mkShapeAttr("x0", values);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(string val) => Interop.mkShapeAttr("x0", val);
        /// Sets the shape's starting x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x0(params string[] values) => Interop.mkShapeAttr("x0", values);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(bool val) => Interop.mkShapeAttr("x1", val);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(params bool[] values) => Interop.mkShapeAttr("x1", values);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(System.DateTime val) => Interop.mkShapeAttr("x1", val);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(params System.DateTime[] values) => Interop.mkShapeAttr("x1", values);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(int val) => Interop.mkShapeAttr("x1", val);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(params int[] values) => Interop.mkShapeAttr("x1", values);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(float val) => Interop.mkShapeAttr("x1", val);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(params float[] values) => Interop.mkShapeAttr("x1", values);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(string val) => Interop.mkShapeAttr("x1", val);
        /// Sets the shape's end x position. See `type` and `xsizemode` for more info.
        public static Box<IShapeProperty> x1(params string[] values) => Interop.mkShapeAttr("x1", values);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(bool val) => Interop.mkShapeAttr("yanchor", val);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(params bool[] values) => Interop.mkShapeAttr("yanchor", values);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(System.DateTime val) => Interop.mkShapeAttr("yanchor", val);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(params System.DateTime[] values) => Interop.mkShapeAttr("yanchor", values);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(int val) => Interop.mkShapeAttr("yanchor", val);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(params int[] values) => Interop.mkShapeAttr("yanchor", values);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(float val) => Interop.mkShapeAttr("yanchor", val);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(params float[] values) => Interop.mkShapeAttr("yanchor", values);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(string val) => Interop.mkShapeAttr("yanchor", val);
        /// Only relevant in conjunction with `ysizemode` set to *pixel*. Specifies the anchor point on the y axis to which `y0`, `y1` and y coordinates within `path` are relative to. E.g. useful to attach a pixel sized shape to a certain data value. No effect when `ysizemode` not set to *pixel*.
        public static Box<IShapeProperty> yanchor(params string[] values) => Interop.mkShapeAttr("yanchor", values);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(bool val) => Interop.mkShapeAttr("y0", val);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(params bool[] values) => Interop.mkShapeAttr("y0", values);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(System.DateTime val) => Interop.mkShapeAttr("y0", val);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(params System.DateTime[] values) => Interop.mkShapeAttr("y0", values);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(int val) => Interop.mkShapeAttr("y0", val);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(params int[] values) => Interop.mkShapeAttr("y0", values);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(float val) => Interop.mkShapeAttr("y0", val);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(params float[] values) => Interop.mkShapeAttr("y0", values);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(string val) => Interop.mkShapeAttr("y0", val);
        /// Sets the shape's starting y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y0(params string[] values) => Interop.mkShapeAttr("y0", values);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(bool val) => Interop.mkShapeAttr("y1", val);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(params bool[] values) => Interop.mkShapeAttr("y1", values);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(System.DateTime val) => Interop.mkShapeAttr("y1", val);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(params System.DateTime[] values) => Interop.mkShapeAttr("y1", values);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(int val) => Interop.mkShapeAttr("y1", val);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(params int[] values) => Interop.mkShapeAttr("y1", values);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(float val) => Interop.mkShapeAttr("y1", val);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(params float[] values) => Interop.mkShapeAttr("y1", values);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(string val) => Interop.mkShapeAttr("y1", val);
        /// Sets the shape's end y position. See `type` and `ysizemode` for more info.
        public static Box<IShapeProperty> y1(params string[] values) => Interop.mkShapeAttr("y1", values);
        /// For `type` *path* - a valid SVG path with the pixel values replaced by data values in `xsizemode`/`ysizemode` being *scaled* and taken unmodified as pixels relative to `xanchor` and `yanchor` in case of *pixel* size mode. There are a few restrictions / quirks only absolute instructions, not relative. So the allowed segments are: M, L, H, V, Q, C, T, S, and Z arcs (A) are not allowed because radius rx and ry are relative. In the future we could consider supporting relative commands, but we would have to decide on how to handle date and log axes. Note that even as is, Q and C Bezier paths that are smooth on linear axes may not be smooth on log, and vice versa. no chained \"polybezier\" commands - specify the segment type for each one. On category axes, values are numbers scaled to the serial numbers of categories because using the categories themselves there would be no way to describe fractional positions On data axes: because space and T are both normal components of path strings, we can't use either to separate date from time parts. Therefore we'll use underscore for this purpose: 2015-02-21_13:45:56.789
        public static Box<IShapeProperty> path(string val) => Interop.mkShapeAttr("path", val);
        /// Sets the opacity of the shape.
        public static Box<IShapeProperty> opacity(int val) => Interop.mkShapeAttr("opacity", val);
        /// Sets the opacity of the shape.
        public static Box<IShapeProperty> opacity(float val) => Interop.mkShapeAttr("opacity", val);
        public static Box<IShapeProperty> line(params Box<ILineProperty>[] properties) => Interop.mkShapeAttr("line", Bindings.flattenProperties(properties));
        /// Sets the color filling the shape's interior.
        public static Box<IShapeProperty> fillcolor(string val) => Interop.mkShapeAttr("fillcolor", val);
        /// When used in a template, named items are created in the output figure in addition to any items the figure already has in this array. You can modify these items in the output figure by making your own item with `templateitemname` matching this `name` alongside your modifications (including `visible: false` or `enabled: false` to hide it). Has no effect outside of a template.
        public static Box<IShapeProperty> name(string val) => Interop.mkShapeAttr("name", val);
        /// Used to refer to a named item in this array in the template. Named items from the template will be created even without a matching item in the input figure, but you can modify one by making an item with `templateitemname` matching its `name`, alongside your modifications (including `visible: false` or `enabled: false` to hide it). If there is no template or no matching item, this item will be hidden unless you explicitly show it with `visible: true`.
        public static Box<IShapeProperty> templateitemname(string val) => Interop.mkShapeAttr("templateitemname", val);
    }

    public static partial class Shape
    {
        /// Specifies the shape type to be drawn. If *line*, a line is drawn from (`x0`,`y0`) to (`x1`,`y1`) with respect to the axes' sizing mode. If *circle*, a circle is drawn from ((`x0`+`x1`)/2, (`y0`+`y1`)/2)) with radius (|(`x0`+`x1`)/2 - `x0`|, |(`y0`+`y1`)/2 -`y0`)|) with respect to the axes' sizing mode. If *rect*, a rectangle is drawn linking (`x0`,`y0`), (`x1`,`y0`), (`x1`,`y1`), (`x0`,`y1`), (`x0`,`y0`) with respect to the axes' sizing mode. If *path*, draw a custom SVG path using `path`. with respect to the axes' sizing mode.
        public static partial class _type
        {
            public static Box<IShapeProperty> circle() => Interop.mkShapeAttr("type", "circle");
            public static Box<IShapeProperty> line() => Interop.mkShapeAttr("type", "line");
            public static Box<IShapeProperty> path() => Interop.mkShapeAttr("type", "path");
            public static Box<IShapeProperty> rect() => Interop.mkShapeAttr("type", "rect");
        }

        /// Specifies whether shapes are drawn below or above traces.
        public static partial class Layer
        {
            public static Box<IShapeProperty> above() => Interop.mkShapeAttr("layer", "above");
            public static Box<IShapeProperty> below() => Interop.mkShapeAttr("layer", "below");
        }

        /// Sets the shape's x coordinate axis. If set to an x axis id (e.g. *x* or *x2*), the `x` position refers to an x coordinate. If set to *paper*, the `x` position refers to the distance from the left side of the plotting area in normalized coordinates where *0* (*1*) corresponds to the left (right) side. If the axis `type` is *log*, then you must take the log of your desired range. If the axis `type` is *date*, then you must convert the date to unix time in milliseconds.
        public static partial class Xref
        {
            public static Box<IShapeProperty> paper() => Interop.mkShapeAttr("xref", "paper");
            public static Box<IShapeProperty> custom(string val) => Interop.mkShapeAttr("xref", val);
            public static Box<IShapeProperty> x(int anchorId) => Interop.mkShapeAttr("xref", anchorId > 1 ? $"x{anchorId}" : "");
        }

        /// Sets the shapes's sizing mode along the x axis. If set to *scaled*, `x0`, `x1` and x coordinates within `path` refer to data values on the x axis or a fraction of the plot area's width (`xref` set to *paper*). If set to *pixel*, `xanchor` specifies the x position in terms of data or plot fraction but `x0`, `x1` and x coordinates within `path` are pixels relative to `xanchor`. This way, the shape can have a fixed width while maintaining a position relative to data or plot fraction.
        public static partial class Xsizemode
        {
            public static Box<IShapeProperty> pixel() => Interop.mkShapeAttr("xsizemode", "pixel");
            public static Box<IShapeProperty> scaled() => Interop.mkShapeAttr("xsizemode", "scaled");
        }

        /// Sets the annotation's y coordinate axis. If set to an y axis id (e.g. *y* or *y2*), the `y` position refers to an y coordinate If set to *paper*, the `y` position refers to the distance from the bottom of the plotting area in normalized coordinates where *0* (*1*) corresponds to the bottom (top).
        public static partial class Yref
        {
            public static Box<IShapeProperty> paper() => Interop.mkShapeAttr("yref", "paper");
            public static Box<IShapeProperty> custom(string val) => Interop.mkShapeAttr("yref", val);
            public static Box<IShapeProperty> y(int anchorId) => Interop.mkShapeAttr("yref", anchorId > 1 ? $"y{anchorId}" : "");
        }

        /// Sets the shapes's sizing mode along the y axis. If set to *scaled*, `y0`, `y1` and y coordinates within `path` refer to data values on the y axis or a fraction of the plot area's height (`yref` set to *paper*). If set to *pixel*, `yanchor` specifies the y position in terms of data or plot fraction but `y0`, `y1` and y coordinates within `path` are pixels relative to `yanchor`. This way, the shape can have a fixed height while maintaining a position relative to data or plot fraction.
        public static partial class Ysizemode
        {
            public static Box<IShapeProperty> pixel() => Interop.mkShapeAttr("ysizemode", "pixel");
            public static Box<IShapeProperty> scaled() => Interop.mkShapeAttr("ysizemode", "scaled");
        }
    }
}