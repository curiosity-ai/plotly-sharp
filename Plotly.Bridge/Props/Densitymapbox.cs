namespace PlotlyBridge
{
    /*////////////////////////////////
    /// THIS FILE IS AUTO-GENERATED //
    ///      by propsDocument       //
    ////////////////////////////////*/
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using Types;
    using Bridge;
    using static Retyped.dom;

    public static partial class Densitymapbox
    {
        /// Sets the opacity of the trace.
        public static Box<IDensitymapboxProperty> opacity(int val) => Interop.mkDensitymapboxAttr("opacity", val);
        /// Sets the opacity of the trace.
        public static Box<IDensitymapboxProperty> opacity(float val) => Interop.mkDensitymapboxAttr("opacity", val);
        /// Sets the trace name. The trace name appear as the legend item and on hover.
        public static Box<IDensitymapboxProperty> name(string val) => Interop.mkDensitymapboxAttr("name", val);
        /// Assign an id to this trace, Use this to provide object constancy between traces during animations and transitions.
        public static Box<IDensitymapboxProperty> uid(string val) => Interop.mkDensitymapboxAttr("uid", val);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(bool val) => Interop.mkDensitymapboxAttr("ids", new[]{val});
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params bool[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(System.DateTime val) => Interop.mkDensitymapboxAttr("ids", new[]{val});
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(float val) => Interop.mkDensitymapboxAttr("ids", new[]{val});
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params float[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(int val) => Interop.mkDensitymapboxAttr("ids", new[]{val});
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params int[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(string val) => Interop.mkDensitymapboxAttr("ids", new[]{val});
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params string[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<bool[]> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(List<bool[]> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params bool[][] values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<IEnumerable<string>> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(List<string[]> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params string[][] values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<IEnumerable<int>> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(List<int[]> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params int[][] values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<IEnumerable<float>> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(List<float[]> values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(params float[][] values) => Interop.mkDensitymapboxAttr("ids", Bindings.flatten2DArrayIf1D(values));
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(Literals.PlotData[] values) => Interop.mkDensitymapboxAttr("ids", values);
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<bool?> values) => Interop.mkDensitymapboxAttr("ids", values.ToArray());
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<System.DateTime?> values) => Interop.mkDensitymapboxAttr("ids", values.ToArray());
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<int?> values) => Interop.mkDensitymapboxAttr("ids", values.ToArray());
        /// Assigns id labels to each datum. These ids for object constancy of data points during animation. Should be an array of strings, not numbers or any other type.
        public static Box<IDensitymapboxProperty> ids(IEnumerable<float?> values) => Interop.mkDensitymapboxAttr("ids", values.ToArray());
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(bool val) => Interop.mkDensitymapboxAttr("customdata", new[]{val});
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params bool[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(System.DateTime val) => Interop.mkDensitymapboxAttr("customdata", new[]{val});
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(float val) => Interop.mkDensitymapboxAttr("customdata", new[]{val});
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params float[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(int val) => Interop.mkDensitymapboxAttr("customdata", new[]{val});
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params int[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(string val) => Interop.mkDensitymapboxAttr("customdata", new[]{val});
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params string[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<bool[]> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(List<bool[]> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params bool[][] values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<IEnumerable<string>> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(List<string[]> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params string[][] values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<IEnumerable<int>> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(List<int[]> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params int[][] values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<IEnumerable<float>> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(List<float[]> values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(params float[][] values) => Interop.mkDensitymapboxAttr("customdata", Bindings.flatten2DArrayIf1D(values));
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(Literals.PlotData[] values) => Interop.mkDensitymapboxAttr("customdata", values);
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<bool?> values) => Interop.mkDensitymapboxAttr("customdata", values.ToArray());
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<System.DateTime?> values) => Interop.mkDensitymapboxAttr("customdata", values.ToArray());
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<int?> values) => Interop.mkDensitymapboxAttr("customdata", values.ToArray());
        /// Assigns extra data each datum. This may be useful when listening to hover, click and selection events. Note that, *scatter* traces also appends customdata items in the markers DOM elements
        public static Box<IDensitymapboxProperty> customdata(IEnumerable<float?> values) => Interop.mkDensitymapboxAttr("customdata", values.ToArray());
        public static Box<IDensitymapboxProperty> hoverlabel(params Box<IHoverlabelProperty>[] properties) => Interop.mkDensitymapboxAttr("hoverlabel", Bindings.flattenProperties(properties));
        public static Box<IDensitymapboxProperty> stream(params Box<IStreamProperty>[] properties) => Interop.mkDensitymapboxAttr("stream", Bindings.flattenProperties(properties));
        public static Box<IDensitymapboxProperty> transforms(params Box<ITransformsProperty>[] properties) => Interop.mkDensitymapboxAttr("transforms", Bindings.flattenPropertiesToArray(properties));
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(bool val) => Interop.mkDensitymapboxAttr("uirevision", val);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(params bool[] values) => Interop.mkDensitymapboxAttr("uirevision", values);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(System.DateTime val) => Interop.mkDensitymapboxAttr("uirevision", val);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("uirevision", values);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(int val) => Interop.mkDensitymapboxAttr("uirevision", val);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(params int[] values) => Interop.mkDensitymapboxAttr("uirevision", values);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(float val) => Interop.mkDensitymapboxAttr("uirevision", val);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(params float[] values) => Interop.mkDensitymapboxAttr("uirevision", values);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(string val) => Interop.mkDensitymapboxAttr("uirevision", val);
        /// Controls persistence of some user-driven changes to the trace: `constraintrange` in `parcoords` traces, as well as some `editable: true` modifications such as `name` and `colorbar.title`. Defaults to `layout.uirevision`. Note that other user-driven trace attribute changes are controlled by `layout` attributes: `trace.visible` is controlled by `layout.legend.uirevision`, `selectedpoints` is controlled by `layout.selectionrevision`, and `colorbar.(x|y)` (accessible with `config: {editable: true}`) is controlled by `layout.editrevision`. Trace changes are tracked by `uid`, which only falls back on trace index if no `uid` is provided. So if your app can add/remove traces before the end of the `data` array, such that the same trace has a different index, you can still preserve user-driven changes if you give each trace a `uid` that stays with it as it moves.
        public static Box<IDensitymapboxProperty> uirevision(params string[] values) => Interop.mkDensitymapboxAttr("uirevision", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(bool val) => Interop.mkDensitymapboxAttr("lon", new[]{val});
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params bool[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(System.DateTime val) => Interop.mkDensitymapboxAttr("lon", new[]{val});
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(float val) => Interop.mkDensitymapboxAttr("lon", new[]{val});
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params float[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(int val) => Interop.mkDensitymapboxAttr("lon", new[]{val});
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params int[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(string val) => Interop.mkDensitymapboxAttr("lon", new[]{val});
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params string[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<bool[]> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(List<bool[]> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params bool[][] values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<IEnumerable<string>> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(List<string[]> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params string[][] values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<IEnumerable<int>> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(List<int[]> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params int[][] values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<IEnumerable<float>> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(List<float[]> values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(params float[][] values) => Interop.mkDensitymapboxAttr("lon", Bindings.flatten2DArrayIf1D(values));
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(Literals.PlotData[] values) => Interop.mkDensitymapboxAttr("lon", values);
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<bool?> values) => Interop.mkDensitymapboxAttr("lon", values.ToArray());
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<System.DateTime?> values) => Interop.mkDensitymapboxAttr("lon", values.ToArray());
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<int?> values) => Interop.mkDensitymapboxAttr("lon", values.ToArray());
        /// Sets the longitude coordinates (in degrees East).
        public static Box<IDensitymapboxProperty> lon(IEnumerable<float?> values) => Interop.mkDensitymapboxAttr("lon", values.ToArray());
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(bool val) => Interop.mkDensitymapboxAttr("lat", new[]{val});
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params bool[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(System.DateTime val) => Interop.mkDensitymapboxAttr("lat", new[]{val});
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(float val) => Interop.mkDensitymapboxAttr("lat", new[]{val});
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params float[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(int val) => Interop.mkDensitymapboxAttr("lat", new[]{val});
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params int[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(string val) => Interop.mkDensitymapboxAttr("lat", new[]{val});
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params string[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<bool[]> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(List<bool[]> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params bool[][] values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<IEnumerable<string>> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(List<string[]> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params string[][] values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<IEnumerable<int>> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(List<int[]> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params int[][] values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<IEnumerable<float>> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(List<float[]> values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(params float[][] values) => Interop.mkDensitymapboxAttr("lat", Bindings.flatten2DArrayIf1D(values));
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(Literals.PlotData[] values) => Interop.mkDensitymapboxAttr("lat", values);
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<bool?> values) => Interop.mkDensitymapboxAttr("lat", values.ToArray());
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<System.DateTime?> values) => Interop.mkDensitymapboxAttr("lat", values.ToArray());
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<int?> values) => Interop.mkDensitymapboxAttr("lat", values.ToArray());
        /// Sets the latitude coordinates (in degrees North).
        public static Box<IDensitymapboxProperty> lat(IEnumerable<float?> values) => Interop.mkDensitymapboxAttr("lat", values.ToArray());
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(bool val) => Interop.mkDensitymapboxAttr("z", new[]{val});
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params bool[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(System.DateTime val) => Interop.mkDensitymapboxAttr("z", new[]{val});
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params System.DateTime[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(float val) => Interop.mkDensitymapboxAttr("z", new[]{val});
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params float[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(int val) => Interop.mkDensitymapboxAttr("z", new[]{val});
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params int[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(string val) => Interop.mkDensitymapboxAttr("z", new[]{val});
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params string[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<bool[]> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(List<bool[]> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params bool[][] values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<IEnumerable<string>> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(List<string[]> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params string[][] values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<IEnumerable<int>> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(List<int[]> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params int[][] values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<IEnumerable<float>> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(List<float[]> values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(params float[][] values) => Interop.mkDensitymapboxAttr("z", Bindings.flatten2DArrayIf1D(values));
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(Literals.PlotData[] values) => Interop.mkDensitymapboxAttr("z", values);
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<bool?> values) => Interop.mkDensitymapboxAttr("z", values.ToArray());
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<System.DateTime?> values) => Interop.mkDensitymapboxAttr("z", values.ToArray());
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<int?> values) => Interop.mkDensitymapboxAttr("z", values.ToArray());
        /// Sets the points' weight. For example, a value of 10 would be equivalent to having 10 points of weight 1 in the same spot
        public static Box<IDensitymapboxProperty> z(IEnumerable<float?> values) => Interop.mkDensitymapboxAttr("z", values.ToArray());
        /// Sets the radius of influence of one `lon` / `lat` point in pixels. Increasing the value makes the densitymapbox trace smoother, but less detailed.
        public static Box<IDensitymapboxProperty> radius(int val) => Interop.mkDensitymapboxAttr("radius", val);
        /// Sets the radius of influence of one `lon` / `lat` point in pixels. Increasing the value makes the densitymapbox trace smoother, but less detailed.
        public static Box<IDensitymapboxProperty> radius(float val) => Interop.mkDensitymapboxAttr("radius", val);
        /// Sets the radius of influence of one `lon` / `lat` point in pixels. Increasing the value makes the densitymapbox trace smoother, but less detailed.
        public static Box<IDensitymapboxProperty> radius(IEnumerable<int> values) => Interop.mkDensitymapboxAttr("radius", values.ToArray());
        /// Sets the radius of influence of one `lon` / `lat` point in pixels. Increasing the value makes the densitymapbox trace smoother, but less detailed.
        public static Box<IDensitymapboxProperty> radius(IEnumerable<float> values) => Interop.mkDensitymapboxAttr("radius", values.ToArray());
        /// Determines if the densitymapbox trace will be inserted before the layer with the specified ID. By default, densitymapbox traces are placed below the first layer of type symbol If set to '', the layer will be inserted above every existing layer.
        public static Box<IDensitymapboxProperty> below(string val) => Interop.mkDensitymapboxAttr("below", val);
        /// Sets text elements associated with each (lon,lat) pair If a single string, the same string appears over all the data points. If an array of string, the items are mapped in order to the this trace's (lon,lat) coordinates. If trace `hoverinfo` contains a *text* flag and *hovertext* is not set, these elements will be seen in the hover labels.
        public static Box<IDensitymapboxProperty> text(string val) => Interop.mkDensitymapboxAttr("text", val);
        /// Sets text elements associated with each (lon,lat) pair If a single string, the same string appears over all the data points. If an array of string, the items are mapped in order to the this trace's (lon,lat) coordinates. If trace `hoverinfo` contains a *text* flag and *hovertext* is not set, these elements will be seen in the hover labels.
        public static Box<IDensitymapboxProperty> text(IEnumerable<string> values) => Interop.mkDensitymapboxAttr("text", values.ToArray());
        /// Sets hover text elements associated with each (lon,lat) pair If a single string, the same string appears over all the data points. If an array of string, the items are mapped in order to the this trace's (lon,lat) coordinates. To be seen, trace `hoverinfo` must contain a *text* flag.
        public static Box<IDensitymapboxProperty> hovertext(string val) => Interop.mkDensitymapboxAttr("hovertext", val);
        /// Sets hover text elements associated with each (lon,lat) pair If a single string, the same string appears over all the data points. If an array of string, the items are mapped in order to the this trace's (lon,lat) coordinates. To be seen, trace `hoverinfo` must contain a *text* flag.
        public static Box<IDensitymapboxProperty> hovertext(IEnumerable<string> values) => Interop.mkDensitymapboxAttr("hovertext", values.ToArray());
        /// Determines which trace information appear on hover. If `none` or `skip` are set, no information is displayed upon hovering. But, if `none` is set, click and hover events are still fired.
        public static Box<IDensitymapboxProperty> hoverinfo(params Box<IDensitymapboxProperty>[] properties) => Interop.mkDensitymapboxAttr("hoverinfo", Bindings.joinEnumProperties(properties));
        /// Template string used for rendering the information that appear on hover box. Note that this will override `hoverinfo`. Variables are inserted using %{variable}, for example \"y: %{y}\". Numbers are formatted using d3-format's syntax %{variable:d3-format}, for example \"Price: %{y:$.2f}\". https://github.com/d3/d3-3.x-api-reference/blob/master/Formatting.md#d3_format for details on the formatting syntax. Dates are formatted using d3-time-format's syntax %{variable|d3-time-format}, for example \"Day: %{2019-01-01|%A}\". https://github.com/d3/d3-3.x-api-reference/blob/master/Time-Formatting.md#format for details on the date formatting syntax. The variables available in `hovertemplate` are the ones emitted as event data described at this link https://plot.ly/javascript/plotlyjs-events/#event-data. Additionally, every attributes that can be specified per-point (the ones that are `arrayOk: true`) are available.  Anything contained in tag `<extra>` is displayed in the secondary box, for example \"<extra>{fullData.name}</extra>\". To hide the secondary box completely, use an empty tag `<extra></extra>`.
        public static Box<IDensitymapboxProperty> hovertemplate(string val) => Interop.mkDensitymapboxAttr("hovertemplate", val);
        /// Template string used for rendering the information that appear on hover box. Note that this will override `hoverinfo`. Variables are inserted using %{variable}, for example \"y: %{y}\". Numbers are formatted using d3-format's syntax %{variable:d3-format}, for example \"Price: %{y:$.2f}\". https://github.com/d3/d3-3.x-api-reference/blob/master/Formatting.md#d3_format for details on the formatting syntax. Dates are formatted using d3-time-format's syntax %{variable|d3-time-format}, for example \"Day: %{2019-01-01|%A}\". https://github.com/d3/d3-3.x-api-reference/blob/master/Time-Formatting.md#format for details on the date formatting syntax. The variables available in `hovertemplate` are the ones emitted as event data described at this link https://plot.ly/javascript/plotlyjs-events/#event-data. Additionally, every attributes that can be specified per-point (the ones that are `arrayOk: true`) are available.  Anything contained in tag `<extra>` is displayed in the secondary box, for example \"<extra>{fullData.name}</extra>\". To hide the secondary box completely, use an empty tag `<extra></extra>`.
        public static Box<IDensitymapboxProperty> hovertemplate(IEnumerable<string> values) => Interop.mkDensitymapboxAttr("hovertemplate", values.ToArray());
        /// Determines whether or not the color domain is computed with respect to the input data (here in `z`) or the bounds set in `zmin` and `zmax`  Defaults to `false` when `zmin` and `zmax` are set by the user.
        public static Box<IDensitymapboxProperty> zauto(bool val) => Interop.mkDensitymapboxAttr("zauto", val);
        /// Sets the lower bound of the color domain. Value should have the same units as in `z` and if set, `zmax` must be set as well.
        public static Box<IDensitymapboxProperty> zmin(int val) => Interop.mkDensitymapboxAttr("zmin", val);
        /// Sets the lower bound of the color domain. Value should have the same units as in `z` and if set, `zmax` must be set as well.
        public static Box<IDensitymapboxProperty> zmin(float val) => Interop.mkDensitymapboxAttr("zmin", val);
        /// Sets the upper bound of the color domain. Value should have the same units as in `z` and if set, `zmin` must be set as well.
        public static Box<IDensitymapboxProperty> zmax(int val) => Interop.mkDensitymapboxAttr("zmax", val);
        /// Sets the upper bound of the color domain. Value should have the same units as in `z` and if set, `zmin` must be set as well.
        public static Box<IDensitymapboxProperty> zmax(float val) => Interop.mkDensitymapboxAttr("zmax", val);
        /// Sets the mid-point of the color domain by scaling `zmin` and/or `zmax` to be equidistant to this point. Value should have the same units as in `z`. Has no effect when `zauto` is `false`.
        public static Box<IDensitymapboxProperty> zmid(int val) => Interop.mkDensitymapboxAttr("zmid", val);
        /// Sets the mid-point of the color domain by scaling `zmin` and/or `zmax` to be equidistant to this point. Value should have the same units as in `z`. Has no effect when `zauto` is `false`.
        public static Box<IDensitymapboxProperty> zmid(float val) => Interop.mkDensitymapboxAttr("zmid", val);
        /// Sets the colorscale. The colorscale must be an array containing arrays mapping a normalized value to an rgb, rgba, hex, hsl, hsv, or named color string. At minimum, a mapping for the lowest (0) and highest (1) values are required. For example, `[[0, 'rgb(0,0,255)'], [1, 'rgb(255,0,0)']]`. To control the bounds of the colorscale in color space, use`zmin` and `zmax`. Alternatively, `colorscale` may be a palette name string of the following list: Greys,YlGnBu,Greens,YlOrRd,Bluered,RdBu,Reds,Blues,Picnic,Rainbow,Portland,Jet,Hot,Blackbody,Earth,Electric,Viridis,Cividis.
        public static Box<IDensitymapboxProperty> colorscale(string val) => Interop.mkDensitymapboxAttr("colorscale", val);
        /// Sets the colorscale. The colorscale must be an array containing arrays mapping a normalized value to an rgb, rgba, hex, hsl, hsv, or named color string. At minimum, a mapping for the lowest (0) and highest (1) values are required. For example, `[[0, 'rgb(0,0,255)'], [1, 'rgb(255,0,0)']]`. To control the bounds of the colorscale in color space, use`zmin` and `zmax`. Alternatively, `colorscale` may be a palette name string of the following list: Greys,YlGnBu,Greens,YlOrRd,Bluered,RdBu,Reds,Blues,Picnic,Rainbow,Portland,Jet,Hot,Blackbody,Earth,Electric,Viridis,Cividis.
        public static Box<IDensitymapboxProperty> colorscale(List<string[]> values) => Interop.mkDensitymapboxAttr("colorscale", Bindings.flatten2DArrayIf1D(values));
        /// Determines whether the colorscale is a default palette (`autocolorscale: true`) or the palette determined by `colorscale`. In case `colorscale` is unspecified or `autocolorscale` is true, the default  palette will be chosen according to whether numbers in the `color` array are all positive, all negative or mixed.
        public static Box<IDensitymapboxProperty> autocolorscale(bool val) => Interop.mkDensitymapboxAttr("autocolorscale", val);
        /// Reverses the color mapping if true. If true, `zmin` will correspond to the last color in the array and `zmax` will correspond to the first color.
        public static Box<IDensitymapboxProperty> reversescale(bool val) => Interop.mkDensitymapboxAttr("reversescale", val);
        /// Determines whether or not a colorbar is displayed for this trace.
        public static Box<IDensitymapboxProperty> showscale(bool val) => Interop.mkDensitymapboxAttr("showscale", val);
        public static Box<IDensitymapboxProperty> colorbar(params Box<IColorbarProperty>[] properties) => Interop.mkDensitymapboxAttr("colorbar", Bindings.flattenProperties(properties));
        /// Sets a reference to a shared color axis. References to these shared color axes are *coloraxis*, *coloraxis2*, *coloraxis3*, etc. Settings for these shared color axes are set in the layout, under `layout.coloraxis`, `layout.coloraxis2`, etc. Note that multiple color scales can be linked to the same color axis.
        public static Box<IDensitymapboxProperty> coloraxis(int anchorId) => Interop.mkDensitymapboxAttr("coloraxis", anchorId > 1 ? $"coloraxis{anchorId}" : "");
        /// Sets a reference to a shared color axis. References to these shared color axes are *coloraxis*, *coloraxis2*, *coloraxis3*, etc. Settings for these shared color axes are set in the layout, under `layout.coloraxis`, `layout.coloraxis2`, etc. Note that multiple color scales can be linked to the same color axis.
        public static Box<IDensitymapboxProperty> coloraxis(string val) => Interop.mkDensitymapboxAttr("coloraxis", val);
        /// Sets a reference between this trace's data coordinates and a mapbox subplot. If *mapbox* (the default value), the data refer to `layout.mapbox`. If *mapbox2*, the data refer to `layout.mapbox2`, and so on.
        public static Box<IDensitymapboxProperty> subplot(int anchorId) => Interop.mkDensitymapboxAttr("subplot", anchorId > 1 ? $"mapbox{anchorId}" : "");
        /// Sets a reference between this trace's data coordinates and a mapbox subplot. If *mapbox* (the default value), the data refer to `layout.mapbox`. If *mapbox2*, the data refer to `layout.mapbox2`, and so on.
        public static Box<IDensitymapboxProperty> subplot(string val) => Interop.mkDensitymapboxAttr("subplot", val);
        /// Sets the source reference on plot.ly for  ids .
        public static Box<IDensitymapboxProperty> idssrc(string val) => Interop.mkDensitymapboxAttr("idssrc", val);
        /// Sets the source reference on plot.ly for  customdata .
        public static Box<IDensitymapboxProperty> customdatasrc(string val) => Interop.mkDensitymapboxAttr("customdatasrc", val);
        /// Sets the source reference on plot.ly for  meta .
        public static Box<IDensitymapboxProperty> metasrc(string val) => Interop.mkDensitymapboxAttr("metasrc", val);
        /// Sets the source reference on plot.ly for  lon .
        public static Box<IDensitymapboxProperty> lonsrc(string val) => Interop.mkDensitymapboxAttr("lonsrc", val);
        /// Sets the source reference on plot.ly for  lat .
        public static Box<IDensitymapboxProperty> latsrc(string val) => Interop.mkDensitymapboxAttr("latsrc", val);
        /// Sets the source reference on plot.ly for  z .
        public static Box<IDensitymapboxProperty> zsrc(string val) => Interop.mkDensitymapboxAttr("zsrc", val);
        /// Sets the source reference on plot.ly for  radius .
        public static Box<IDensitymapboxProperty> radiussrc(string val) => Interop.mkDensitymapboxAttr("radiussrc", val);
        /// Sets the source reference on plot.ly for  text .
        public static Box<IDensitymapboxProperty> textsrc(string val) => Interop.mkDensitymapboxAttr("textsrc", val);
        /// Sets the source reference on plot.ly for  hovertext .
        public static Box<IDensitymapboxProperty> hovertextsrc(string val) => Interop.mkDensitymapboxAttr("hovertextsrc", val);
        /// Sets the source reference on plot.ly for  hoverinfo .
        public static Box<IDensitymapboxProperty> hoverinfosrc(string val) => Interop.mkDensitymapboxAttr("hoverinfosrc", val);
        /// Sets the source reference on plot.ly for  hovertemplate .
        public static Box<IDensitymapboxProperty> hovertemplatesrc(string val) => Interop.mkDensitymapboxAttr("hovertemplatesrc", val);
    }

    public static partial class Densitymapbox
    {
        /// Determines whether or not this trace is visible. If *legendonly*, the trace is not drawn, but can appear as a legend item (provided that the legend itself is visible).
        public static partial class Visible
        {
            public static Box<IDensitymapboxProperty> legendonly() => Interop.mkDensitymapboxAttr("visible", "legendonly");
            public static Box<IDensitymapboxProperty> _false() => Interop.mkDensitymapboxAttr("visible", false);
            public static Box<IDensitymapboxProperty> _true() => Interop.mkDensitymapboxAttr("visible", true);
        }

        /// Determines which trace information appear on hover. If `none` or `skip` are set, no information is displayed upon hovering. But, if `none` is set, click and hover events are still fired.
        public static partial class Hoverinfo
        {
            public static Box<IDensitymapboxProperty> all() => Interop.mkDensitymapboxAttr("hoverinfo", "all");
            public static Box<IDensitymapboxProperty> none() => Interop.mkDensitymapboxAttr("hoverinfo", "none");
            public static Box<IDensitymapboxProperty> skip() => Interop.mkDensitymapboxAttr("hoverinfo", "skip");
            public static Box<IDensitymapboxProperty> lat() => Interop.mkDensitymapboxAttr("hoverinfo", "lat");
            public static Box<IDensitymapboxProperty> lon() => Interop.mkDensitymapboxAttr("hoverinfo", "lon");
            public static Box<IDensitymapboxProperty> name() => Interop.mkDensitymapboxAttr("hoverinfo", "name");
            public static Box<IDensitymapboxProperty> text() => Interop.mkDensitymapboxAttr("hoverinfo", "text");
            public static Box<IDensitymapboxProperty> z() => Interop.mkDensitymapboxAttr("hoverinfo", "z");
        }
    }
}